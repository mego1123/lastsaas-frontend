import { RouteObject } from "react-router";

/**
 * Ghost routes — placeholder (lastsaas doesn't use ghost routes)
 * Auth pages are in publicRoutes instead
 */
const ghostRoutes: RouteObject = {
  id: "ghost",
  children: [],
};

export { ghostRoutes };
